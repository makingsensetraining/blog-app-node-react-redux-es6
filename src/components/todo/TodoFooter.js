import React, {PropTypes} from 'react';

const TodoFooter = (props) => {
    return (
        <div>
            x items completed || Filters: Completed | Active
        </div>
    );
};

TodoFooter.propTypes = {
    //
};

export default TodoFooter;
